import { ForignAsyncPipe } from './forign-async.pipe';

describe('ForignAsyncPipe', () => {
  it('create an instance', () => {
    // const pipe = new ForignAsyncPipe();
    // expect(pipe).toBeTruthy();
  });
});
